<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.miniblog.mapper.CommentLikeMapper">

    <select id="selectByUserIdAndCommentId" resultType="com.miniblog.entity.CommentLike">
        SELECT * FROM comment_like
        WHERE user_id = #{userId} AND comment_id = #{commentId}
    </select>

    <select id="countByCommentId" resultType="int">
        SELECT COUNT(*) FROM comment_like WHERE comment_id = #{commentId}
    </select>

    <delete id="deleteByUserIdAndCommentId">
        DELETE FROM comment_like WHERE user_id = #{userId} AND comment_id = #{commentId}
    </delete>

</mapper>
